# rapports/

Les rapports générés (`donnees-actuelles.md`) contiennent des chiffres
réels sur l'opération de la banque — ignorés par git par défaut (voir
`.gitignore` à la racine). Ce README documente la convention, pas le
contenu.

Régénérer : `python scripts/analyser.py`
